node_modules/
package-lock.json

# Variables de entorno / credenciales
.env
.env.*
!.env.example

# Capturas generadas por scripts/verify-comparator-browser.js (verificacion
# manual local, no es parte del repo).
.verify-screenshots/
